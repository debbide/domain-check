version: '3.8'

services:
  domain-check:
    build: .
    container_name: domain-check
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - ./data:/app/data
    environment:
      - PASSWORD=${PASSWORD:-123123}
      - PORT=${PORT:-3000}
